repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.12.7
  hooks:
    - id: ruff-check
      args: [ --fix ]
    - id: ruff-check
      args: [ --select=I, --fix ]
    - id: ruff-format
    # Use ruff format on markdown files
    - id: ruff-format
      types_or: [markdown]
      require_serial: true
      entry: doccmd --language python --no-pad-file --command "ruff format"
      additional_dependencies: ["doccmd==2026.2.27.1", "click-compose==2025.10.27.3"]
- repo: https://github.com/astral-sh/uv-pre-commit
  rev: 0.12.11
  hooks:
    - id: uv-lock
